import {
  getSprite,
  img2canvas
} from "../../utils";

/**
 * @class
 */
export default class ShadowEntity {

  /**
   * @param {TextureEntity} texture
   * @constructor
   */
  constructor(texture) {
    this.buffer = null;
    console.log(texture);
  }

}